apiVersion: v1
kind: Service
metadata:
  name: {{ include "hermesgate.fullname" . }}
  labels:
    {{- include "hermesgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hermesgate.selectorLabels" . | nindent 4 }}
